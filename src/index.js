import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// This is the example of components that means like javascript function and element refers a React return 
// local is props here that pass through clock components





// eslint-disable-next-line no-undef
ReactDOM.render(<App />, document.getElementById('root'));